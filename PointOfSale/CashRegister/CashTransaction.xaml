<UserControl x:Class="PointOfSale.CashRegister.CashTransaction"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PointOfSale.CashRegister"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="75"/>
        </Grid.RowDefinitions>
        <local:CashRegisterControl Grid.Row="0"></local:CashRegisterControl>
        <TextBlock Grid.Row="1" Text="{Binding Path=Total, StringFormat=c}" VerticalAlignment="Bottom" HorizontalAlignment="Center" FontSize="50" Name="Total"></TextBlock>
        <Button Grid.Row="1" Width="150" HorizontalAlignment="Right" Name="TenderButton">Tender</Button>
    </Grid>
</UserControl>
